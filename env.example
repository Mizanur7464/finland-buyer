# ============================================
# Solana Copy Trading Bot Configuration
# ============================================
# Copy this file to .env and fill in your values
# DO NOT commit .env file to git (it contains sensitive data)

# ============================================
# REQUIRED CONFIGURATION
# ============================================

# Master wallet address to copy trades from
# Get this from the buyer - the wallet you want to monitor and copy
MASTER_WALLET_ADDRESS=

# Your trading wallet's encrypted private key
# Run: python encrypt_wallet.py to encrypt your private key
# Then paste the encrypted key here
PRIVATE_KEY_ENCRYPTED=

# Yellow Stone Geyser gRPC endpoint URL
# QuickNode format: grpcs://endpoint-name.solana-network.quiknode.pro:10000
# Example: grpcs://smart-holy-pool.solana-testnet.quiknode.pro:10000
# Get this from the buyer
YELLOWSTONE_GRPC_URL=

# Yellow Stone Geyser gRPC authentication token
# This is the token from your QuickNode endpoint URL
# Example: b345ed06a4a2e7ceffd4d4f4547f7ce63da840a9
# Get this from the buyer
YELLOWSTONE_GRPC_TOKEN=

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Solana RPC endpoint (for transaction submission)
# QuickNode format: https://endpoint-name.solana-network.quiknode.pro/token/
# Testnet (default):
RPC_ENDPOINT=https://api.testnet.solana.com
# Or use your QuickNode RPC endpoint:
# RPC_ENDPOINT=https://smart-holy-pool.solana-testnet.quiknode.pro/b345ed06a4a2e7ceffd4d4f4547f7ce63da840a9/
# Mainnet (when ready):
# RPC_ENDPOINT=https://api.mainnet-beta.solana.com

# Network: testnet or mainnet
# Start with testnet for testing
NETWORK=testnet

# Slippage tolerance percentage (default: 1.0%)
# Higher = more tolerance for price changes, but may get worse prices
SLIPPAGE_TOLERANCE=1.0

# Transaction fee buffer in SOL (default: 0.001)
# Extra SOL kept aside for transaction fees
FEE_BUFFER=0.001

# Tips/priority fee per transaction in SOL (default: 0.0001)
# Higher tips = faster transaction execution (same-block execution)
TIPS_AMOUNT=0.0001

# Lot Size Configuration
# LOT_SIZE_MODE: "fixed", "percentage", or "multiplier"
#   - "fixed": Always trade fixed amount (use LOT_SIZE_VALUE as SOL amount)
#   - "percentage": Trade percentage of master wallet amount (use LOT_SIZE_VALUE as percentage, e.g., 10 for 10%)
#   - "multiplier": Multiply master wallet amount (use LOT_SIZE_VALUE as multiplier, e.g., 2 for 2x)
# LOT_SIZE_VALUE: Number value based on mode
# Examples:
#   LOT_SIZE_MODE=percentage
#   LOT_SIZE_VALUE=10
#   (This means: trade 10% of master wallet's amount)
LOT_SIZE_MODE=percentage
LOT_SIZE_VALUE=10.0

# ============================================
# TELEGRAM BOT (Optional - for notifications)
# ============================================

# Telegram bot token (get from @BotFather on Telegram)
# Leave empty if you don't want Telegram notifications
# TELEGRAM_BOT_TOKEN=

# Your Telegram chat ID (where to receive notifications)
# Send a message to your bot, then visit: https://api.telegram.org/bot<TOKEN>/getUpdates
# TELEGRAM_CHAT_ID=

# ============================================
# WALLET ENCRYPTION PASSWORD (Optional)
# ============================================
# If you want to use a custom password for wallet encryption
# Leave empty to use default (less secure, change in production)
# WALLET_PASSWORD=

